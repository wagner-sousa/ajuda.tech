# Conversa — Ajuda Tech

| Campo         | Valor                    |
|---------------|--------------------------|
| **Data**      | 2026-05-23               |
| **Hora**      | 00:49:52                 |
| **Mensagens** | 2                        |

---

## Mensagem 1

**User:**

Ajuste para o chat deixar o path `/chat` e ficar no index (`/`)

---

## Mensagem 2

**Assistant:**

Analisou os arquivos de roteamento e fez as seguintes alterações para mover o chat de `/chat/` para `/`:

### Arquivos alterados

**`ajuda_tech/urls.py`** — removeu `core.urls` e moveu `chat.urls` para a raiz:

```python
# Antes
urlpatterns = [
    path("", include("core.urls")),
    path("chat/", include("chat.urls")),
]

# Depois
urlpatterns = [
    path("", include("chat.urls")),
]
```

**`chat/static/chat/js/chatApi.js`** — atualizou o endpoint:

```js
// Antes
export const CHAT_ENDPOINT = '/chat/send/';

// Depois
export const CHAT_ENDPOINT = '/send/';
```

**`chat/static/chat/js/chatApi.errors.test.js`** — atualizou expectativa de URL:

```js
// Antes
expect(CHAT_ENDPOINT).toBe('/chat/send/');

// Depois
expect(CHAT_ENDPOINT).toBe('/send/');
```

**`chat/static/chat/js/chatApp.test.js`** — atualizou mock:

```js
// Antes
vi.mock('./chatApi.js', () => ({
  CHAT_ENDPOINT: '/chat/send/',

// Depois
vi.mock('./chatApi.js', () => ({
  CHAT_ENDPOINT: '/send/',
```

### Rotas resultantes

| Rota          | Destino                    |
|---------------|----------------------------|
| `/`           | Chat (ChatView)            |
| `/send/`      | API de mensagens           |
| `/recommend/` | API de recomendação        |

Validação: `python manage.py check` retornou **0 issues**.
