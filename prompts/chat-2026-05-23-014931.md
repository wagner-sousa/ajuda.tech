# Conversa — Ajuda Tech

**Data:** 2026-05-23  
**Hora:** 01:49:31  
**Total de mensagens:** 4  

---

## Mensagem 1

**Usuário:**

Enquanto o openrouter retornar erro 429 tente automaticamente.

---

## Mensagem 2

**Assistente:**

Leitura de `chat/services.py` para entender a lógica de retry existente.

O código já retentava em 429, mas o `RateLimitError` consumia o mesmo contador `attempt` do `max_retries` geral (padrão 2). Foram realizadas as seguintes alterações:

### Alterações em `chat/services.py`

**1. Nova constante:**
```python
_DEFAULT_MAX_RATE_LIMIT_RETRIES = 10
```

**2. Novo parâmetro no construtor:**
```python
def __init__(self, api_key: str | None = None, max_retries: int = _DEFAULT_MAX_RETRIES, max_rate_limit_retries: int = _DEFAULT_MAX_RATE_LIMIT_RETRIES):
    ...
    self.max_rate_limit_retries: int = max_rate_limit_retries
```

**3. Contador separado em `_chat_completion`:**
```python
attempt = 0
rate_limit_attempt = 0
last_exc: Exception | None = None

while attempt <= self.max_retries:
    try:
        ...
        return self._handle_response(response)

    except RateLimitError as exc:
        rate_limit_attempt += 1
        last_exc = exc
        if rate_limit_attempt <= self.max_rate_limit_retries:
            wait = exc.retry_after
            logger.warning(
                "Rate limit atingido (429) — tentativa %d/%d. Aguardando %ds.",
                rate_limit_attempt,
                self.max_rate_limit_retries,
                wait,
            )
            time.sleep(wait)
            # Não incrementa `attempt`: 429 tem contador próprio
        else:
            raise
```

**Resultado:** 44 testes passando.

**Comportamento após a mudança:**
- Erros 429 **não consomem** as tentativas gerais de falha de rede/servidor
- Até 10 tentativas exclusivas para 429, aguardando o tempo indicado pelo header `Retry-After`
- Ao esgotar as 10 tentativas, lança `RateLimitError` normalmente
- O parâmetro é configurável: `OpenRouterClient(max_rate_limit_retries=20)`

---

## Mensagem 3

**Usuário:**

Compartilhou o arquivo `logs/errors.log` mostrando o comportamento das retentativas em produção, com as linhas:

```
WARNING 2026-05-22 20:36:48,025 services Rate limit atingido (429) — tentativa 1/10. Aguardando 1s.
WARNING 2026-05-22 20:36:49,026 services Rate limit atingido (429) — tentativa 2/10. Aguardando 1s.
...
WARNING 2026-05-22 20:36:57,034 services Rate limit atingido (429) — tentativa 10/10. Aguardando 1s.
```

O log confirma que a implementação está funcionando corretamente.

---

## Mensagem 4

**Usuário:**

Solicitou salvar a conversa atual em um arquivo markdown no diretório `/prompts`, seguindo o prompt `save-current-prompt.prompt.md`.

---

## Código modificado — `chat/services.py` (trecho relevante)

```python
_DEFAULT_MAX_RETRIES = 2
_DEFAULT_MAX_RATE_LIMIT_RETRIES = 10
_RETRYABLE_STATUS_CODES = frozenset({500, 502, 503, 504})


class OpenRouterClient:
    def __init__(
        self,
        api_key: str | None = None,
        max_retries: int = _DEFAULT_MAX_RETRIES,
        max_rate_limit_retries: int = _DEFAULT_MAX_RATE_LIMIT_RETRIES,
    ):
        ...
        self.max_retries: int = max_retries
        self.max_rate_limit_retries: int = max_rate_limit_retries

    def _chat_completion(self, messages: list[dict]) -> str:
        """Executa a chamada HTTP com retry e retorna o conteúdo da resposta."""
        attempt = 0
        rate_limit_attempt = 0
        last_exc: Exception | None = None

        while attempt <= self.max_retries:
            try:
                ...
                return self._handle_response(response)

            except (ServiceUnavailableError,) as exc:
                last_exc = exc
                if attempt < self.max_retries:
                    wait = 2**attempt
                    logger.warning(
                        "OpenRouter indisponível (tentativa %d/%d). Aguardando %ds.",
                        attempt + 1,
                        self.max_retries + 1,
                        wait,
                    )
                    time.sleep(wait)
                attempt += 1

            except (AuthenticationError, InvalidResponseError):
                raise  # sem retry para erros permanentes

            except RateLimitError as exc:
                rate_limit_attempt += 1
                last_exc = exc
                if rate_limit_attempt <= self.max_rate_limit_retries:
                    wait = exc.retry_after
                    logger.warning(
                        "Rate limit atingido (429) — tentativa %d/%d. Aguardando %ds.",
                        rate_limit_attempt,
                        self.max_rate_limit_retries,
                        wait,
                    )
                    time.sleep(wait)
                    # Não incrementa `attempt`: 429 tem contador próprio
                else:
                    raise

            except requests.exceptions.Timeout as exc:
                last_exc = ServiceUnavailableError(f"Timeout após {self.timeout}s: {exc}")
                if attempt < self.max_retries:
                    wait = 2**attempt
                    logger.warning(
                        "Timeout na tentativa %d/%d. Aguardando %ds.",
                        attempt + 1,
                        self.max_retries + 1,
                        wait,
                    )
                    time.sleep(wait)
                attempt += 1

            except requests.exceptions.ConnectionError as exc:
                last_exc = ServiceUnavailableError(f"Erro de conexão: {exc}")
                if attempt < self.max_retries:
                    wait = 2**attempt
                    time.sleep(wait)
                attempt += 1

        raise last_exc
```
