# Conversa — Ajuda Tech

| Campo         | Valor                  |
|---------------|------------------------|
| **Data**      | 2026-05-23             |
| **Hora**      | 00:48:23               |
| **Mensagens** | 4                      |

---

## Mensagem 1

**Usuário:**

Ajuste o prompt do projeto retornar resposta mais curtas e diretas

---

## Mensagem 2

**Assistente:**

Leitura do arquivo `chat/prompts.py` para entender o conteúdo atual do `SYSTEM_PROMPT`.

Foram feitas duas alterações:

**1. `chat/prompts.py` — `SYSTEM_PROMPT` atualizado:**

```python
SYSTEM_PROMPT = """\
Você é Herbert, um assistente especialista em tecnologia da Ajuda Tech.
Sua missão é ajudar pessoas leigas a escolher o computador ideal para suas necessidades.

Diretrizes de conversa:
- Respostas curtas e diretas — máximo 2 a 3 frases por mensagem.
- Faça apenas UMA pergunta por vez.
- Use linguagem simples e amigável. Evite jargões técnicos.
- Colete: finalidade de uso, mobilidade, orçamento e exigência de desempenho.
- Quando tiver as informações, ofereça recomendação clara e objetiva em linguagem simples.
- Nunca mencione especificações técnicas sem explicar o que significam na prática.
- Ao encerrar com recomendação, informe que o usuário pode solicitar a lista de produtos.
"""
```

**2. `chat/services.py` — payload da API com `max_tokens` e `temperature`:**

```python
is_extraction = any(
    PRODUCT_EXTRACTION_PROMPT[:50] in m.get("content", "")
    for m in messages
)
max_tokens = 800 if is_extraction else 300
response = requests.post(
    _OPENROUTER_URL,
    headers=self._build_headers(),
    data=json.dumps({
        "model": self.model,
        "messages": messages,
        "max_tokens": max_tokens,
        "temperature": 0.7,
    }),
    timeout=self.timeout,
)
```

Resumo das mudanças:
- `SYSTEM_PROMPT` instrui explicitamente respostas de no máximo 2-3 frases e uma pergunta por vez.
- Payload da API inclui `max_tokens: 300` nas perguntas do chat (e `800` na extração de produtos) e `temperature: 0.7`.

---

## Mensagem 3

**Usuário:**

*(Solicitação de salvar a conversa em arquivo markdown — prompt `save-current-prompt.prompt.md`)*

---

## Mensagem 4

**Assistente:**

Arquivo salvo em `/workspaces/ajuda.tech/prompts/chat-2026-05-23-004823.md`.
