# Conversa — Ajuda Tech: Integração OpenRouter com TDD

**Data:** 2026-05-22  
**Hora:** 20:51:28  
**Total de mensagens:** 4 (2 do usuário, 2 do assistente)  
**Projeto:** `/home/wagner/github/ajuda.tech`

---

## Mensagem 1

**Usuário:**

> Você é um desenvolvedor backend Python especializado em Django e integrações com APIs externas. Sua tarefa é implementar uma integração com o OpenRouter seguindo rigorosamente a metodologia TDD (Test-Driven Development), com foco em um chatbot que retorna uma lista de produtos.
>
> **Requisitos técnicos:**
> - Framework: Django
> - Use o SDK do OpenRouter (https://openrouter.ai/docs/quickstart)
> - Linguagem: Python
> - Metodologia: TDD (escrever testes antes da implementação)
> - Nível de completeness: Production-ready (testes completos, tratamento de erros robusto, documentação clara)
>
> **Fluxo de trabalho obrigatório:**
> 1. Comece escrevendo testes unitários que definem o comportamento esperado da integração
> 2. Implemente o código mínimo necessário para passar nos testes
> 3. Refatore o código mantendo os testes passando
>
> **O que deve ser implementado:**
> - Uma classe ou serviço Django que encapsule a comunicação com a API OpenRouter
> - Tratamento de autenticação (usando chave de API)
> - Métodos para fazer requisições à API e processar respostas para retornar lista de produtos
> - Tratamento robusto de erros e exceções
> - Validação de respostas da API
> - Testes que cubram os casos de sucesso e falha
>
> **Padrões de código:**
> - Siga as convenções Django (estrutura de apps, models, services)
> - Use variáveis de ambiente para a chave de API (via `.env` ou `settings.py`)
> - Implemente logging apropriado
> - Garanta que o código seja testável e siga princípios SOLID
>
> **Entregáveis:**
> - Arquivo(s) de teste contendo todos os testes escritos
> - Arquivo(s) de implementação com a integração funcional e production-ready
> - Breve explicação do fluxo TDD seguido
> - Instruções claras sobre como executar os testes
>
> Comece pelos testes. Mostre o arquivo de teste primeiro, depois a implementação que passa neles.

---

## Mensagem 2

**Assistente:**

O assistente explorou o código existente do projeto, leu a documentação do OpenRouter SDK e implementou a integração completa seguindo a metodologia TDD em três fases.

### Fase Red — Testes escritos antes da implementação

Foram criados três arquivos de teste:

**`chat/tests/test_services.py`** — 44 testes unitários para `OpenRouterClient`:

```python
"""
Testes unitários para chat.services.OpenRouterClient
Metodologia: TDD — estes testes foram escritos ANTES da implementação.

Cobertura:
  - Inicialização e leitura de configurações
  - Construção de cabeçalhos HTTP
  - Chamada à API OpenRouter e processamento de resposta
  - Tratamento de erros HTTP (401, 429, 5xx)
  - Lógica de retry com backoff exponencial
  - Extração e parsing de recomendações de produtos
"""

import json
import pytest
from unittest.mock import patch, MagicMock, call

import requests as req_module

from chat.exceptions import (
    AuthenticationError,
    InvalidResponseError,
    RateLimitError,
    ServiceUnavailableError,
)
from chat.services import OpenRouterClient

# ─── Helpers ──────────────────────────────────────────────────────────────────

def make_mock_response(status_code=200, json_data=None):
    """Cria um mock de requests.Response com status_code e corpo JSON."""
    mock = MagicMock()
    mock.status_code = status_code
    if json_data is not None:
        mock.json.return_value = json_data
    else:
        mock.json.side_effect = ValueError("No JSON")
    return mock


def make_chat_response(content="resposta de teste"):
    """Cria o payload JSON padrão de uma resposta bem-sucedida do OpenRouter."""
    return {
        "choices": [
            {"message": {"role": "assistant", "content": content}}
        ]
    }


@pytest.fixture
def client():
    return OpenRouterClient(api_key="test-api-key")


class TestOpenRouterClientInit:
    def test_init_stores_explicit_api_key(self, client): ...
    def test_init_reads_api_key_from_django_settings(self, settings): ...
    def test_init_raises_improperly_configured_when_key_missing(self, settings): ...
    def test_explicit_key_takes_precedence_over_settings(self, settings): ...
    def test_reads_model_from_settings(self, settings): ...
    def test_reads_timeout_from_settings(self, settings): ...
    def test_default_model_is_set(self): ...
    def test_default_timeout_is_positive(self): ...

class TestBuildHeaders:
    def test_authorization_uses_bearer_scheme(self, client): ...
    def test_content_type_is_application_json(self, client): ...
    def test_http_referer_header_is_present(self, client): ...
    def test_x_title_header_is_present(self, client): ...

class TestChatCompletion:
    def test_returns_assistant_content_string(self, mock_post, client): ...
    def test_first_message_is_system_prompt(self, mock_post, client): ...
    def test_system_prompt_mentions_herbert(self, mock_post, client): ...
    def test_conversation_history_follows_system_message(self, mock_post, client): ...
    def test_uses_model_from_client_attribute(self, mock_post, client): ...
    def test_passes_timeout_to_requests(self, mock_post, client): ...
    def test_posts_to_correct_endpoint(self, mock_post, client): ...

class TestHttpErrorHandling:
    def test_401_raises_authentication_error(self, mock_post, client): ...
    def test_429_raises_rate_limit_error(self, mock_post, client): ...
    def test_500_raises_service_unavailable(self, mock_post, client): ...
    def test_503_raises_service_unavailable(self, mock_post, client): ...
    def test_unexpected_4xx_raises_invalid_response(self, mock_post, client): ...
    def test_non_json_body_raises_invalid_response(self, mock_post, client): ...
    def test_empty_choices_raises_invalid_response(self, mock_post, client): ...
    def test_missing_choices_key_raises_invalid_response(self, mock_post, client): ...
    def test_requests_timeout_raises_service_unavailable(self, mock_post, client): ...
    def test_connection_error_raises_service_unavailable(self, mock_post, client): ...
    def test_401_does_not_retry(self, mock_post, client): ...

class TestRetryLogic:
    def test_retries_up_to_max_retries_on_timeout(self, mock_post, mock_sleep, client): ...
    def test_succeeds_on_second_attempt(self, mock_post, mock_sleep, client): ...
    def test_exponential_backoff_between_retries(self, mock_post, mock_sleep, client): ...
    def test_retries_on_500_server_error(self, mock_post, mock_sleep, client): ...

class TestProductRecommendations:
    def test_returns_a_list(self, mock_post, client): ...
    def test_returns_three_products(self, mock_post, client): ...
    def test_each_product_has_required_keys(self, mock_post, client): ...
    def test_products_cover_ideal_budget_and_premium_options(self, mock_post, client): ...
    def test_raises_invalid_response_when_no_json_array_found(self, mock_post, client): ...
    def test_raises_invalid_response_when_json_is_object_not_list(self, mock_post, client): ...
    def test_parses_json_wrapped_in_markdown_code_block(self, mock_post, client): ...
    def test_parses_json_with_leading_text(self, mock_post, client): ...
    def test_sends_extraction_prompt_as_last_user_message(self, mock_post, client): ...
    def test_includes_conversation_history_in_payload(self, mock_post, client): ...
```

**`chat/tests/test_models.py`** — 16 testes para `Conversation` e `Message`:

- `TestConversationModel`: criação, unicidade de session_key, defaults, timestamps, `get_history()`
- `TestMessageModel`: criação de mensagens, choices, cascade delete

**`chat/tests/test_views.py`** — 20 testes de integração:

- `TestChatView`: status 200, template correto, CSRF presente
- `TestSendMessageView`: JSON válido, persistência de mensagens, validação de input, tratamento de erros (400, 405, 500, 503)
- `TestRecommendView`: lista de produtos, histórico passado ao serviço, tratamento de erros

### Confirmação de fase Red

```
ERROR chat/tests/test_services.py
E   ModuleNotFoundError: No module named 'chat.exceptions'
ERROR chat/tests/test_views.py
E   ModuleNotFoundError: No module named 'chat.exceptions'
```

### Fase Green — Implementação

**Arquivos criados:**

#### `chat/exceptions.py`

```python
class OpenRouterError(Exception):
    """Exceção base para todas as falhas relacionadas ao OpenRouter."""

class AuthenticationError(OpenRouterError):
    """Chave de API ausente, inválida ou sem permissão (HTTP 401/403)."""

class RateLimitError(OpenRouterError):
    """Limite de requisições excedido (HTTP 429)."""

class ServiceUnavailableError(OpenRouterError):
    """API indisponível, timeout ou erro de rede (HTTP 5xx, Timeout, ConnectionError)."""

class InvalidResponseError(OpenRouterError):
    """A resposta da API não pôde ser interpretada (JSON inválido, estrutura inesperada)."""
```

#### `chat/models.py`

```python
from django.db import models

class Conversation(models.Model):
    session_key = models.CharField(max_length=40, unique=True, db_index=True)
    is_completed = models.BooleanField(default=False)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def get_history(self) -> list[dict]:
        return list(self.messages.values("role", "content").order_by("created_at"))

class Message(models.Model):
    ROLE_CHOICES = [("user", "Usuário"), ("assistant", "Assistente")]
    conversation = models.ForeignKey(Conversation, on_delete=models.CASCADE, related_name="messages")
    role = models.CharField(max_length=10, choices=ROLE_CHOICES)
    content = models.TextField()
    created_at = models.DateTimeField(auto_now_add=True)
```

#### `chat/prompts.py`

```python
SYSTEM_PROMPT = """\
Você é Herbert, um assistente especialista em tecnologia da Ajuda Tech.
Sua missão é ajudar pessoas leigas a escolher o computador ideal para suas necessidades.
[...]
"""

PRODUCT_EXTRACTION_PROMPT = """\
Com base em toda a conversa acima, gere uma lista de exatamente 3 produtos recomendados
(opções "budget", "ideal" e "premium") no seguinte formato JSON puro:
[{ "name": ..., "price": ..., "type": ..., "specs": ..., "justification": ..., "option": ... }]
"""
```

#### `chat/services.py`

```python
import json, logging, re, time
import requests
from django.conf import settings
from django.core.exceptions import ImproperlyConfigured

_OPENROUTER_URL = "https://openrouter.ai/api/v1/chat/completions"
_RETRYABLE_STATUS_CODES = frozenset({500, 502, 503, 504})

class OpenRouterClient:
    def __init__(self, api_key=None, max_retries=2):
        # Lê LLM_API_KEY do settings; levanta ImproperlyConfigured se ausente
        ...

    def chat_completion(self, messages: list[dict]) -> str:
        # Prepende system prompt → chama _chat_completion → retorna string
        ...

    def get_product_recommendations(self, history: list[dict]) -> list[dict]:
        # _build_extraction_messages → _chat_completion → _parse_products
        ...

    def _build_headers(self) -> dict:
        # Authorization: Bearer {key}, Content-Type, HTTP-Referer, X-Title
        ...

    def _chat_completion(self, messages) -> str:
        # POST para OpenRouter com retry exponencial
        # Retry: timeout, ConnectionError, 5xx
        # Sem retry: 401, 429, 4xx inesperado
        ...

    def _handle_response(self, response) -> str:
        # 401 → AuthenticationError
        # 429 → RateLimitError
        # 5xx → ServiceUnavailableError
        # 4xx → InvalidResponseError
        # JSON inválido → InvalidResponseError
        # choices[] vazio → InvalidResponseError
        ...

    def _parse_products(self, content: str) -> list[dict]:
        # Extrai JSON de: bloco markdown ```json```, texto com prefixo, ou JSON puro
        # Lança InvalidResponseError se não encontrar array
        ...
```

#### `chat/views.py`

```python
class ChatView(TemplateView):
    template_name = "chat/chat.html"

class SendMessageView(View):
    # POST /chat/send/ — valida, salva, chama IA, retorna {"reply": "..."}
    # 400 se message ausente/vazio, 405 para GET, 503/500 em erros de serviço
    ...

class RecommendView(View):
    # POST /chat/recommend/ — usa histórico da sessão, retorna {"products": [...]}
    ...
```

#### Demais arquivos criados

| Arquivo | Conteúdo |
|---|---|
| `ajuda_tech/settings.py` | Config Django + `LLM_API_KEY`, `LLM_MODEL`, `LLM_TIMEOUT` via `python-decouple` |
| `ajuda_tech/urls.py` | Rotas raiz → `core.urls` e `chat/` → `chat.urls` |
| `ajuda_tech/wsgi.py` | Ponto de entrada WSGI |
| `chat/urls.py` | `app_name="chat"` com rotas `chat`, `send_message`, `recommend` |
| `chat/admin.py` | `ConversationAdmin`, `MessageAdmin` |
| `chat/apps.py` | `ChatConfig` |
| `chat/templates/chat/chat.html` | Template com formulário de chat + JS para fetch |
| `pytest.ini` | `DJANGO_SETTINGS_MODULE = ajuda_tech.settings` |
| `conftest.py` | Setup do ambiente Django para testes |

### Resultado — 80/80 testes passando

```
============================== 80 passed in 9.92s ==============================
```

```
chat/tests/test_models.py   16 PASSED
chat/tests/test_views.py    20 PASSED
chat/tests/test_services.py 44 PASSED
```

---

## Mensagem 3

**Usuário:**

> Siga as instruções em `save-current-prompt.prompt.md` para salvar esta conversa.

---

## Mensagem 4

**Assistente:**

Conversação salva neste arquivo: `prompts/chat-2026-05-22-205128.md`

---

## Como executar os testes

```bash
# Ativar ambiente virtual
source venv/bin/activate

# Todos os testes
python3 -m pytest chat/tests/ -v

# Apenas serviço OpenRouter
python3 -m pytest chat/tests/test_services.py -v

# Filtrar por grupo
python3 -m pytest chat/tests/ -v -k "Retry"
python3 -m pytest chat/tests/ -v -k "Product"

# Com cobertura (se pytest-cov instalado)
python3 -m pytest chat/tests/ --cov=chat --cov-report=term-missing
```
