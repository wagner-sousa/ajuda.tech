# Sessão de Desenvolvimento — Ajuda Tech
**Data:** 20 de maio de 2026  
**Arquivo em foco:** `chat/services.py`

---

## Contexto do Projeto

**Ajuda Tech** — assistente conversacional com IA que ajuda usuários leigos a escolherem o computador ideal.

**Stack:** Python 3.12+, Django 5.x, SQLite, OpenRouter API

### Estrutura relevante
```
chat/
  services.py      # Cliente OpenRouter — timeout, retentativas
  prompts.py       # System Prompts Herbert (leigo→técnico)
  exceptions.py    # Hierarquia de exceções customizadas
  models.py        # Conversation, Message
  tests/
    test_services.py
    test_models.py
    test_views.py
```

---

## Atividade da Sessão

### Testes executados

```bash
cd /home/wagner/github/ajuda.tech && source venv/bin/activate
python3 -m pytest chat/tests/ -v --tb=short
```

**Resultado:** ✅ Exit Code 0 — todos os testes passaram.

---

## Arquivo Principal: `chat/services.py`

Cliente OpenRouter com as seguintes responsabilidades:

- Autenticação via Bearer token (`settings.LLM_API_KEY`)
- Envio de mensagens à API `/chat/completions` do OpenRouter
- Retry com backoff exponencial em falhas transitórias (timeout, 5xx)
- Sem retry para erros permanentes (401, 429, 4xx inesperado)
- Parsing de recomendações de produtos a partir da resposta da IA

### Configurações padrão

| Configuração        | Valor padrão                            |
|---------------------|-----------------------------------------|
| `_DEFAULT_MODEL`    | `deepseek/deepseek-v4-flash:free`       |
| `_DEFAULT_TIMEOUT`  | `60` segundos                           |
| `_DEFAULT_MAX_RETRIES` | `2`                                  |
| `_RETRYABLE_STATUS_CODES` | `{500, 502, 503, 504}`          |

### Classe `OpenRouterClient`

```python
class OpenRouterClient:
    def __init__(self, api_key: str | None = None, max_retries: int = _DEFAULT_MAX_RETRIES):
        # Lê LLM_API_KEY de settings ou parâmetro explícito
        # ImproperlyConfigured se ausente

    def chat_completion(self, messages: list[dict]) -> str:
        # Envia histórico e retorna resposta do assistente

    # Retry com backoff exponencial para status 5xx
```

---

## Variáveis de Ambiente Obrigatórias

| Variável       | Descrição                            |
|----------------|--------------------------------------|
| `SECRET_KEY`   | Chave secreta do Django              |
| `LLM_API_KEY`  | Chave da API do provedor LLM         |
| `LLM_MODEL`    | Modelo usado (ex: `deepseek/...`)    |
| `LLM_TIMEOUT`  | Timeout em segundos (padrão: 60)     |
| `DEBUG`        | `True` (dev) / `False` (prod)        |

---

## Conversa

**Usuário:** Follow instructions in #prompt:SKILL.md with these arguments: Salve o chat atual em um arquivo markdown na pasta /prompts

**Assistente:** Criou este arquivo em `prompts/sessao-2026-05-20.md` com o contexto da sessão atual.

---

**Usuário:** Follow instructions in #prompt:SKILL.md with these arguments: Obtenha o chat atual e salve em um arquivo markdown na pasta #file:prompts

**Assistente:** Leu `prompts.md` (template de System Prompt do Herbert) e o arquivo de sessão existente. Identificou que `sessao-2026-05-20.md` já documenta a sessão do dia. Adicionou este turno ao histórico de conversa do mesmo arquivo.
